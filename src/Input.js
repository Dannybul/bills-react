import React from "react";

const Input = (props) => {
  return (
    <div class="input">
      <input type="text" placeholder="Enter bill subject here"></input>
      <button type="submit">Submit</button>
    </div>
  );
};

export default Input;
